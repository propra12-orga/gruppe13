
public class Figur {
	/*
	 * // neues Object. Die Grundlage fuer Figuren Mutterklasse von Bomberman
	 * und // Monstern. private int xposition; private int yposition;
	 * 
	 * 
	 * public Figur(int xposition, int yposition) { this.xposition = xposition;
	 * this.yposition = yposition; }
	 * 
	 * 
	 * @SuppressWarnings("unused") private int getxPosition() { return
	 * xposition; }
	 * 
	 * private void setxPosition(int xposition) { this.xposition = xposition; }
	 * 
	 * @SuppressWarnings("unused") private int getyPosition() { return
	 * yposition; }
	 * 
	 * private void setyPosition(int yposition) { this.yposition = yposition; }
	 * 
	 * @SuppressWarnings("unused") private void links(int anzahl) { // fehlt
	 * ueberpruefung auf wegfrei. setxPosition(xposition - anzahl);
	 * 
	 * }
	 * 
	 * @SuppressWarnings("unused") private void rechts(int anzahl) { // fehlt
	 * ueberpruefung auf wegfrei. setxPosition(xposition + anzahl);
	 * 
	 * }
	 * 
	 * @SuppressWarnings("unused") private void oben(int anzahl) { // fehlt
	 * ueberpruefung auf wegfrei. setyPosition(yposition + anzahl);
	 * 
	 * }
	 * 
	 * @SuppressWarnings("unused") private void unten(int anzahl) { // fehlt
	 * ueberpruefung auf wegfrei. setyPosition(yposition - anzahl);
	 * 
	 * }
	 * 
	 * // TESTS
	 * 
	 * @Test public void linksgehe() { Figur a = new Figur(5, 5); a.links(2);
	 * assertEquals(3, a.getxPosition()); }
	 */
}
